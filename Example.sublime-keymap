[
    // Windows - copy the matching binding below to your user keymap to activate:
    { "keys": ["ctrl+alt+shift+f"], "command": "search_in_project", "args": { "type": "search" } },

    // OSX
    { "keys": ["alt+super+shift+f"], "command": "search_in_project", "args": { "type": "search" } },

    // Linux
    { "keys": ["alt+ctrl+shift+f"], "command": "search_in_project", "args": { "type": "search" } }
]
